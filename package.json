{
  "name": "habitify-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run preflight && wrangler deploy",
    "test": "vitest run --passWithNoTests && vitest run --config test/generated-artifacts.vitest.config.ts",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.node-scripts.json",
    "preflight": "node scripts/preflight.mjs",
    "generate:settings-manifest": "vitest run --config scripts/generators.vitest.config.ts scripts/generate-settings-manifest.vitest.ts",
    "generate:readme-integrations": "vitest run --config scripts/generators.vitest.config.ts scripts/generate-readme-integrations.vitest.ts",
    "generate": "npm run generate:settings-manifest && npm run generate:readme-integrations"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.0",
    "@cloudflare/workers-types": "^4.20260101.0",
    "@types/node": "^26.1.2",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "wrangler": "~4.107.1"
  }
}
